image: ghcr.io/linuxserver/daapd
network_mode: host
environment:
  - PGID=${GROUP_ID}
  - PUID=${USER_ID}
volumes:
  - ${CONFIG}/multiroom-audio/spotifymat:/music/spotify
  - ${CONFIG}/daapd/mat:/config
restart: unless-stopped