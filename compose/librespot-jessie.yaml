build: /config/librespot
image: librespot:jessie
restart: unless-stopped
volumes:
  - ${CONFIG}/multi-room/spotifyjessie:/audio/pipe
environment:
  - SPOTIFY_NAME=Apartment
  - SPOTIFY_BITRATE=320
  - SPOTIFY_USER=${spotify_user}
  - SPOTIFY_PASS=${spotify_password}
ports:
  - "6969:6969"