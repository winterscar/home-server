image: ghcr.io/linuxserver/daapd
network_mode: host
environment:
  - PGID=${GROUP_ID}
  - PUID=${USER_ID}
volumes:
  - ${CONFIG}/multiroom-audio/spotifyjessie:/music/spotify
  - ${CONFIG}/daapd/jessie/forked-daapd-jessie.conf:/config/forked-daapd.conf
restart: unless-stopped