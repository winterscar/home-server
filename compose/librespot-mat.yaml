build: /config/librespot
image: librespot
restart: unless-stopped
volumes:
  - ${CONFIG}/multiroom-audio/spotifymat:/audio/pipe
environment:
  - SPOTIFY_NAME=Apartment
  - SPOTIFY_BITRATE=320
  - SPOTIFY_USER=${spotify_user}
  - SPOTIFY_PASS=${spotify_password}
ports:
  - "6970:6969"