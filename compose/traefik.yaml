ports:
  - '8080:8080'
  - '80:80'
volumes:
  - '${CONFIG}/traefik/traefik.yml:/etc/traefik/traefik.yml'
  - '/var/run/docker.sock:/var/run/docker.sock'
labels:
  - "traefik.enable=true"
  - "traefik.http.routers.traefik.rule=Host(`traefik.rancher.pasquet.co`)"
image: 'traefik:v2.2'
